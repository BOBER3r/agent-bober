{"contractId":"sprint-spec-20260814-pge-full-convergence-2","status":"complete","iteration":1,
"commits":["7237c56 - give the golden dataset a durable commit approval, so commit and finalize execute","275f074 - pin commit's durable-approval behaviour at the node level"],
"route":"Route A (a concurrent approver writing a real temp-plus-rename approval file while the run blocks polling — the pattern interrupt.test.ts's startApprover already uses) PLUS Route C's process.cwd() fix (a run-root-scoped DiskCheckpointMechanism swapped into the registry for one case's run, restored in a finally). Route B (teaching disk.ts to honour a pre-existing marker) was NOT taken, so disk.ts:80-83's race-safety sweep keeps its full meaning. disk.ts is byte-untouched.",
"lever":"checkpointOverrides['end-of-pipeline']='disk' via a SECOND code-constant pinned config goldenApprovedConfig(), selected per case through one allowlisted input shape {approved:true}. goldenConfig() is byte-identical — proven by the six pre-existing replay cases re-capturing byte-for-byte. The graph's other hitl gate (plan_clarify/post-plan) stays on the autopilot default.",
"safety_layers":["Every golden run root is a fresh mkdtemp temp dir, never the checkout.","The only disk instance a durable-approval run can reach is one pointed at that run's own root, swapped in for the run's duration and restored in a finally — pinned by a test asserting getCheckpointMechanism('disk') is the SAME reference before and after, and that .bober/approvals never appears under REPO_ROOT.","A REPLAY's effect registry never calls inner.invoke at all, so no live git command is reachable from a replay case regardless.","Only the CAPTURE step can reach a real effect seam, and its committer binding is the fixed '0000000' stub from whole-graph.ts, never commitAll."],
"orchestrator_verified_safety":"Independently confirmed: HEAD unchanged after running the full gate, .bober/approvals absent from the checkout, all runs in /var/folders/.../golden-* temp dirs, and git diff over interrupt.ts, interpreter.ts, checkpoints/, finalize.ts, nodes/commit.ts and pge-engine.ts is EMPTY.",
"blast_radius":"EXACT match to the prediction, verified by programmatic JSON diff (not eyeballing) against replay-full-run-evaluation-passes.json. Only terminalNodeId (graceful_failure->finalize), audits (all approved; mechanism disk/disk/noop), pipelineResult[0].errors (present->absent), and the tail pinnedResponses entry (graceful_failure/run.gracefulFailure -> commit/git.commit) differ. completionMarker, history, contracts, specs, progress, runState, briefings, reviews, evalResults byte-identical; pipelineResult[0].success still true (Option A frozen). The six pre-existing replay cases are byte-identical.",
"coverage":"NEVER_EXECUTED is now ['context_compact','critique','rework_route','synthesize']. Coverage 40/44 (~90.9%). The two-directional mutation-proof block is byte-for-byte untouched and still passes.",
"golden_count":"6 -> 7 replay cases (44 total). Gate 7/7 at 100%.",
"open_defect_surfaced":"finalize.ts:220-221's mechanism-label asymmetry is now directly visible in the new case's third audit line ('noop' where the first two read 'disk'), because finalizePipelineRun asks end-of-pipeline a third time independently. Pre-existing, deliberately untouched, now VISIBLE in a committed fixture.",
"blockers":[]}
