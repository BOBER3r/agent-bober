{"contractId":"sprint-spec-20260812-terminal-vocabulary-5","status":"complete","iteration":1,
"commits":["ce94992 - flip runSprintCycle and the workflow flusher to write completed","0c68422 - fix getStatusIcon so a settled sprint doesn't render [PENDING]","2cd3612 - close the contracts status delta; record the other three","87f23ee - migrate the two remaining ts-side passed writers","061ff25 - sync docs and inline prose with the write flip","14efc5f - docs for the write flip and the honest contracts disposition"],
"sc_5_2_disposition":{"status":"CLOSED — both engines write 'completed', pinned by asserting tsContract.status against pgeContract.status directly rather than against a literal.","evaluatorFeedback":"RECORDED, not closed — PGE has no writer anywhere in src/pge/ (zero grep hits outside tests); closing means adding a writer to a PGE node body, a graph-node change outside scope.","generatorNotes":"RECORDED, not closed — same reason.","version":"RECORDED, not closed — sprint_exit writes version:attempts, runSprintCycle writes none, and VOLATILE_KEYS deliberately excludes version because stripping it would hide a real divergence.","net":"contracts stays in the pinned 4-field divergence set, with 3 deltas remaining inside it (was 4)."},
"getStatusIcon":"Fixed a real, contract-unnamed defect: it switched on the bare literal 'passed' and would have silently rendered every settled sprint as [PENDING] once the write flipped. Exported, routed through isSettledContractStatus, pinned with 6 tests that ITERATE SETTLED_CONTRACT_STATUSES rather than hardcoding literals, so a future third settled word cannot reintroduce the gap.",
"extra_writers":"Migrated BOTH mcp/tools/sprint.ts:255 and cli/commands/sprint.ts:285, with two pinning tests. Judged in-scope because the spec is titled 'One terminal vocabulary', sprint 6 is docs-only so nothing later would cover them, and the corpus guard already promises zero 'passed' contracts going forward.",
"golden":"ZERO of 43 cases moved — git diff --stat .bober/golden/ empty after GOLDEN_CAPTURE=1. Exactly the curator's prediction: capture drives PgeEngine only and this sprint touched no PGE code.",
"negative_control":"Reverted executor.test.ts's fraction-based 'stops reproducing its expectation' control to a % 7 divisor with the non-vacuity guard removed and the exit assertion changed to GOLDEN_EXIT.pass. Confirmed it goes GREEN while corrupting nothing — the exact false pass it exists to prevent. Reverted via git checkout --, verified byte-identical against a pre-edit backup with diff -q, re-ran the real control: 16/16 green.",
"beyond_worklist_finding":"src/orchestrator/workflow/interpreter.test.ts:246 was MISCLASSIFIED by the curator's own site table as an 'outcome, not status' read. It is actually a genuine SprintContract.status assertion read through a real RunResultFlusher.flush() in an integration test. Caught only by running the full contracts/orchestrator/mcp sweep before declaring the worklist done. Fixed and documented inline.",
"left_alone_verified_by_reading":["flusher.ts:62","interpreter.ts:128/134/140/141 (SprintOutcomeKind)","pipeline.ts:598 (history event name)","pipeline.ts:614 (telemetry enum)","pge/runtime/commit.ts:539 (was :533; migrating would move golden cases, forbidden by sc-5-4's stop condition)","pge/runtime/interpreter.ts:728 (verdictFrom, same reasoning)"],
"suite":"459 files / 6960 tests passed, 2 pre-existing skips, 0 failed (baseline 6952, +8 new tests). Golden 6/6.",
"environment":"Two transient ENOSPC/ENOTEMPTY failures mid-verification (abort-run.test.ts, run-manager.test.ts) from the machine's disk hitting 100%. ORCHESTRATOR CONFIRMED: disk is at 89% with 2.1GiB free. Both files are run-abort machinery this sprint never touches; both passed in isolation and in the final full-suite re-run.",
"blockers":[]}
