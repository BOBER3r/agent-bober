{"contractId":"sprint-spec-20260812-terminal-vocabulary-3","status":"complete","iteration":1,
"commits":["19ad3d4 - bober(sprint-3): add a monotone version field to SprintContract, written at sprint_exit","83ceed1 - bober(sprint-3): re-capture golden replay cases for the new version field"],
"source_chosen":"The local `attempts` in sprintExitNode (src/pge/nodes/sprint-review.ts): Math.max(1, state.evaluations.filter(e => e.contractId === contract.contractId && e.verdict !== 'skipped').length), written as `version: attempts`.",
"proofs_by_execution":["Two consecutive runSprint calls over an identical fixture both wrote version 1; asserted equal in a new test.","Golden re-capture of replay-full-run-evaluation-fails (two sprint_exit calls, callIndex 0 and 1, same branchKey) produced version:2 for BOTH — attempts stayed at its settled value across the idempotent second exit rather than double-counting.","sc-3-4 built a seeded copy with 'version' in seeded === false and a settled copy with an IDENTICAL updatedAt via createFixedClock; replaceIfNewer.merge picks settled in both directions. A counterfactual control shows that WITHOUT version the seeded copy wins — so the ordering is attributable to version."],
"anti_default":"version is z.number().int().min(0).optional() with NO .default(). The anti-default test asserts 'version' in result.data === false for a contract without one, and would fail immediately if a default were introduced.",
"golden_recapture":"Exactly the 5 predicted enforcement:'replay' cases (replay-plan-clarify-rounds-exhausted has no sprint_exit pin and was correctly untouched). Every diff hunk read directly: each adds only a version line inside pinnedResponses[].request.contract; expected.artifacts byte-identical in all 5. Stat: 5 files, 12 insertions, 6 deletions.",
"conformance_note":"conformance.engines.test.ts prose updated from 'three field deltas' to FOUR — the new one is version (pgeContract.version defined, tsContract.version undefined).",
"scope":"reducers.ts untouched (git diff --stat empty). The sprint-evaluate.test.ts KNOWN LIMITATION assertion is unchanged and still passes — sprint 4's job.",
"blockers":[]}
