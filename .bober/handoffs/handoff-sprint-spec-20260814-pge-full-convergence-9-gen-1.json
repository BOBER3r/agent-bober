{
  "handoffId": "handoff-sprint-spec-20260814-pge-full-convergence-9-gen-1",
  "type": "to-generator",
  "contractId": "sprint-spec-20260814-pge-full-convergence-9",
  "specId": "spec-20260814-pge-full-convergence",
  "timestamp": "2026-08-14T16:54:55Z",
  "iteration": 1,
  "context": {
    "projectOverview": "agent-bober: TypeScript multi-agent pipeline finishing the PGE graph engine. Default engine stays 'ts'.",
    "completedSprints": [
      "1-3 checkpoints/gated effects/audits-unreachable(ADR)",
      "4 history CLOSED",
      "5 evaluatorFeedback+generatorNotes CLOSED",
      "6 version CLOSED, contracts converges; pipelineResult.errors architectural",
      "7 dead 'passed' comparisons retired",
      "8 critique + rework_route execute (coverage 40/44 -> 42/44); context_compact recorded structurally blocked"
    ],
    "currentBranch": "bober/pge-full-convergence",
    "coverageState": "42/44. NEVER_EXECUTED = ['context_compact','synthesize']. 8 of 45 golden cases are enforcement=replay.",
    "threeStructuralLimitsSoFar": "audits (sprint 3), pipelineResult.errors (sprint 6), context_compact (sprint 8) \u2014 all evaluator-confirmed, all the same KIND of finding.",
    "relevantFiles": [
      "src/pge/golden/coverage.test.ts",
      ".bober/golden/",
      "src/pge/golden/capture.test.ts",
      "src/pge/topology/coding.graph.ts",
      "src/pge/nodes/root.ts"
    ]
  },
  "contract": {
    "contractId": "sprint-spec-20260814-pge-full-convergence-9",
    "specId": "spec-20260814-pge-full-convergence",
    "sprintNumber": 9,
    "title": "rework_route and synthesize execute \u2014 NEVER_EXECUTED empties",
    "description": "The last two unreached nodes. When they execute, every node in the committed topology has run at least once.",
    "status": "in-progress",
    "dependsOn": [
      "sprint-spec-20260814-pge-full-convergence-8"
    ],
    "features": [
      "feat-6"
    ],
    "successCriteria": [
      {
        "criterionId": "sc-9-1",
        "description": "A golden case drives rework_route to execution with span status 'ok'.",
        "verificationMethod": "unit-test",
        "required": true
      },
      {
        "criterionId": "sc-9-2",
        "description": "A golden case drives synthesize to execution with span status 'ok'.",
        "verificationMethod": "unit-test",
        "required": true
      },
      {
        "criterionId": "sc-9-3",
        "description": "NEVER_EXECUTED is empty, and the guard that fails when a node re-enters it is still present and still bites.",
        "verificationMethod": "unit-test",
        "required": true
      },
      {
        "criterionId": "sc-9-4",
        "description": "Node coverage reports every node in the committed topology as executed, asserted against the artifact rather than a hardcoded count.",
        "verificationMethod": "unit-test",
        "required": true
      },
      {
        "criterionId": "sc-9-5",
        "description": "Full suite, typecheck, typecheck:tests, lint and build green, and the golden gate passing at or above its replay floor.",
        "verificationMethod": "build",
        "required": true
      }
    ],
    "nonGoals": [
      "Relaxing the span-status-ok rule to make a node count as covered."
    ],
    "stopConditions": [
      "A node is genuinely unreachable in the committed topology \u2014 that is a topology finding worth more than the coverage number; record it."
    ],
    "definitionOfDone": "All required criteria met, the full suite plus typecheck, typecheck:tests, lint and build green, the golden gate passing, and every documentation claim added backed by a test that fails when the claim stops being true.",
    "assumptions": [],
    "outOfScope": [],
    "ambiguityScore": 3,
    "generatorNotes": "With the list empty, the guard's shape changes: it can no longer assert 'these are unreachable and here is why'. Replace it with the equivalent positive claim \u2014 every node executed \u2014 keeping a control that fails if coverage regresses. Do not simply delete the guard. Run the suite as `npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**'` \u2014 a bare run picks up nested worktrees. Verify on a CLEAN checkout, not just the working tree: this repo has twice been green locally and red in CI because uncommitted state was carrying the result.",
    "evaluatorNotes": "An empty list is exactly where a guard rots into a no-op. Verify the replacement genuinely fails when a node stops executing \u2014 remove a case in a scratch edit and confirm it reports. Run the suite as `npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**'` \u2014 a bare run picks up nested worktrees. Verify on a CLEAN checkout, not just the working tree: this repo has twice been green locally and red in CI because uncommitted state was carrying the result.",
    "estimatedFiles": [
      "src/pge/golden/coverage.test.ts",
      ".bober/golden/",
      "src/pge/golden/capture.test.ts"
    ],
    "estimatedDuration": "medium",
    "iterationHistory": [],
    "createdAt": "2026-08-14T00:00:00Z",
    "updatedAt": "2026-08-14T16:54:55Z",
    "preFlightFinding": {
      "foundBy": "orchestrator, from sprint 8's evaluator-confirmed structural finding",
      "at": "2026-08-14T16:54:55Z",
      "sc-9-1": "ALREADY SATISFIED by sprint 8. rework_route executes with span status 'ok' (superstep 42, route.label 'exhausted'), because critique's sole successor edge IS rework_route. Confirm it still holds; do not re-derive it.",
      "sc-9-3": "UNSATISFIABLE AS WRITTEN. NEVER_EXECUTED cannot empty: context_compact is structurally unreachable (shipped supervisor has no code path returning COMPACT_LABEL; supervisor.reads excludes 'messages'). Evaluator-confirmed in sprint 8 by fresh trace.",
      "sc-9-4": "UNSATISFIABLE AS WRITTEN for the same reason \u2014 'every node executed' cannot hold while context_compact is structurally blocked.",
      "stopConditionApplies": "The contract's own stopCondition covers this: 'A node is genuinely unreachable in the committed topology \u2014 that is a topology finding worth more than the coverage number; record it.'",
      "amendedIntent": "sc-9-3/sc-9-4 become: NEVER_EXECUTED contains ONLY nodes proven structurally unreachable, each with a recorded reason and a claim test; and coverage asserts every node executes EXCEPT those, computed against the topology ARTIFACT rather than a hardcoded count. The guard must still bite in BOTH directions \u2014 fail if a covered node stops executing, AND fail if a listed node starts executing without the list being updated. Per generatorNotes: do NOT simply delete the guard.",
      "realWork": "synthesize (sc-9-2) is the genuine remaining target."
    }
  },
  "config": {
    "commands": {
      "build": "npm run build",
      "typecheck": "npm run typecheck",
      "typecheckTests": "npm run typecheck:tests",
      "lint": "npm run lint",
      "test": "npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**'",
      "golden": "GOLDEN_PASS_THRESHOLD=80 node scripts/run-golden-regression.mjs"
    },
    "generator": {
      "model": "claude-opus-5",
      "maxTurnsPerSprint": 50,
      "autoCommit": true,
      "branchPattern": "bober/{feature-name}",
      "provider": "anthropic"
    }
  },
  "principles": "# Project Principles\n\n> These principles guide all planning, generation, and evaluation in the Bober pipeline.\n> Updated: 2026-04-16\n\n## Mission\n\nAgent-bober is a multi-agent harness that builds applications autonomously using any LLM. It orchestrates a pipeline of specialized agents (Researcher, Planner, Curator, Generator, Evaluator) and supports Claude, GPT, Gemini, and Ollama. It ships as an npm CLI + MCP server for Claude Code, Cursor, and Windsurf.\n\n## Users\n\n- **Primary:** Developers using Claude Code, Cursor, or Windsurf who want autonomous feature generation with quality gates\n- **Secondary:** Teams integrating agent-bober programmatically via the TypeScript API or MCP server\n- Technical level: intermediate to advanced \u2014 users understand LLMs, CI/CD, and modern JS tooling\n\n## Quality Standards\n\n- **Type safety:** TypeScript strict mode with all strict flags (`noUnusedLocals`, `noUnusedParameters`, `noImplicitReturns`, `noFallthroughCasesInSwitch`, `isolatedModules`). Zero type errors is a hard gate.\n- **Lint compliance:** ESLint flat config with `consistent-type-imports` enforced, `no-explicit-any` warned, unused vars errored (with `_` prefix escape). Zero lint errors is a hard gate.\n- **Test coverage:** Vitest for unit tests. Tests are collocated with source (`*.test.ts` next to `*.ts`). Tests run against the real project when practical (e.g., scanner tests scan agent-bober itself).\n- **Build integrity:** `tsc` must produce clean output. Build is a required evaluator strategy.\n\n## Technical Principles\n\n### Follow\n\n- **ESM everywhere.** `\"type\": \"module\"` in package.json. All imports use `.js` extensions for NodeNext resolution. No CommonJS.\n- **Provider-agnostic interfaces.** All LLM interaction goes through `providers/types.ts` \u2014 provider-specific SDKs are wrapped by adapters (`anthropic.ts`, `openai.ts`, `google.ts`). Never leak SDK types outside adapter files.\n- **Zod for config validation.** All configuration schemas are Zod schemas in `config/schema.ts`. Runtime config loading uses `z.parse()`. No hand-rolled validation.\n- **Contract-based architecture.** PlanSpecs, SprintContracts, and EvalResults are the communication protocol between agents. They live in `contracts/` and are JSON files on disk in `.bober/`.\n- **Filesystem state.** All mutable state (specs, contracts, handoffs, eval results, history) is stored as JSON files in `.bober/`. No database, no in-memory global state.\n- **Section comments.** Use unicode box-drawing section headers: `// -- Section Name ------` to organize long files.\n- **Small utility modules.** Utils in `utils/` are focused single-purpose files (`fs.ts`, `git.ts`, `logger.ts`). Keep them small.\n- **Conventional commits.** Format: `type: description` where type is `feat`, `fix`, `docs`, `release`, `refactor`, `test`, `chore`. Sprint-generated commits use `bober(sprint-N): description`.\n- **Use `type` imports.** ESLint enforces `consistent-type-imports` -- import types with `import type { ... }` syntax.\n- **Prefix unused params with `_`.** The `_` prefix is the only escape hatch for unused variables/parameters.\n\n### Avoid\n\n- **No `any` without justification.** `no-explicit-any` is a warning; aim for zero. Use `unknown` + type narrowing instead.\n- **No SDK lock-in.** Never import `@anthropic-ai/sdk` or `openai` outside of their respective adapter files in `providers/`.\n- **No synchronous filesystem ops.** All fs operations use `node:fs/promises`. No `fs.readFileSync` etc.\n- **No barrel re-exports for deep internals.** `src/index.ts` exports the public API only. Internal modules import directly.\n- **No test mocks for filesystem.** Tests that need filesystem state create temp directories and clean up. The scanner tests run against the real codebase.\n\n## Design Principles\n\nN/A -- no user interface. Agent-bober is a CLI tool and programmatic library. The MCP server exposes tools, not UI.\n",
  "evaluatorFeedback": null
}