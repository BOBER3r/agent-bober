{
  "handoffId": "handoff-sprint-spec-20260814-pge-full-convergence-5-gen-2",
  "type": "to-generator",
  "contractId": "sprint-spec-20260814-pge-full-convergence-5",
  "specId": "spec-20260814-pge-full-convergence",
  "timestamp": "2026-08-14T14:08:05Z",
  "iteration": 2,
  "context": {
    "projectOverview": "agent-bober: TypeScript multi-agent pipeline. This spec finishes the PGE graph engine. The default engine deliberately stays 'ts'.",
    "completedSprints": [
      {
        "contractId": "...-1",
        "title": "Checkpoints inside the sprint fan-out \u2014 the ADR decision",
        "summary": "ADR for per-branch checkpoints; runtime cannot express a per-branch interrupt."
      },
      {
        "contractId": "...-2",
        "title": "Gated effects execute under a durable approval",
        "summary": "commit and finalize execute for the first time under a real durable approval."
      },
      {
        "contractId": "...-3",
        "title": "audits converges",
        "summary": "PROVED architecturally unreachable; ADR-backed permanent acceptance. Golden 6/6 -> 7/7."
      },
      {
        "contractId": "...-4",
        "title": "history converges \u2014 the missing phase events",
        "summary": "JUST COMPLETED (072f814..cf7595d + docs 021fe97). Added src/pge/runtime/history.ts emitPhaseEvent delegating to the EXISTING appendHistory, plus HISTORY_EVENT_NODE_MAP; nine emitters at real node lifecycle boundaries; new generateAttemptsSoFar in gates.ts. Divergence set 4 -> 3: now ['audits','contracts','pipelineResult']."
      }
    ],
    "currentBranch": "bober/pge-full-convergence",
    "importantNote": "Sprint 4 landed TODAY and shifted lines in src/pge/nodes/*.ts and conformance.engines.test.ts. Read CURRENT file state; do not trust line numbers from the contract text.",
    "relevantFiles": [
      "src/pge/nodes/sprint-review.ts",
      "src/pge/nodes/sprint-evaluate.ts",
      "src/orchestrator/pipeline.ts",
      "src/orchestrator/workflow/conformance.engines.test.ts",
      ".bober/golden/"
    ]
  },
  "contract": {
    "contractId": "sprint-spec-20260814-pge-full-convergence-5",
    "specId": "spec-20260814-pge-full-convergence",
    "sprintNumber": 5,
    "title": "PGE writes evaluatorFeedback and generatorNotes",
    "description": "Two of the three contract deltas exist because no PGE node writes these fields at all. Give the settled contract the same feedback and notes an imperative run records.",
    "status": "in-progress",
    "dependsOn": [],
    "features": [
      "feat-5"
    ],
    "successCriteria": [
      {
        "criterionId": "sc-5-1",
        "description": "A settled contract from a graph run carries evaluatorFeedback with the same content an imperative run records for the same outcome.",
        "verificationMethod": "unit-test",
        "required": true
      },
      {
        "criterionId": "sc-5-2",
        "description": "The same holds for generatorNotes.",
        "verificationMethod": "unit-test",
        "required": true
      },
      {
        "criterionId": "sc-5-3",
        "description": "Both are written from the node that owns the information, not copied from the seeded contract.",
        "verificationMethod": "unit-test",
        "required": true
      },
      {
        "criterionId": "sc-5-4",
        "description": "The contracts divergence is now down to `version` alone \u2014 asserted, not assumed.",
        "verificationMethod": "unit-test",
        "required": true
      },
      {
        "criterionId": "sc-5-5",
        "description": "Golden cases whose artifacts move are re-captured in this sprint, diff hunks read.",
        "verificationMethod": "unit-test",
        "required": true
      },
      {
        "criterionId": "sc-5-6",
        "description": "Full suite, typecheck, typecheck:tests, lint and build green, and the golden gate passing at or above its replay floor.",
        "verificationMethod": "build",
        "required": true
      }
    ],
    "nonGoals": [
      "Closing the version delta \u2014 sprint 6.",
      "Changing what the evaluator decides or what feedback it produces."
    ],
    "stopConditions": [
      "The graph engine genuinely lacks the information one of these fields needs \u2014 say which and why rather than writing a plausible-looking placeholder."
    ],
    "definitionOfDone": "All required criteria met, the full suite plus typecheck, typecheck:tests, lint and build green, the golden gate passing, and every documentation claim added backed by a test that fails when the claim stops being true.",
    "assumptions": [],
    "outOfScope": [],
    "ambiguityScore": 3,
    "generatorNotes": "The imperative writer is src/orchestrator/pipeline.ts around the settle site (it sets evaluatorFeedback from evaluation.summary). The graph equivalent settles at src/pge/nodes/sprint-review.ts:203-223. Write the fields there, from the same source of truth the imperative engine uses, so the two cannot drift by construction. Run the suite as `npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**'` \u2014 a bare run picks up nested worktrees. Verify on a CLEAN checkout, not just the working tree: this repo has twice been green locally and red in CI because uncommitted state was carrying the result.",
    "evaluatorNotes": "Check the values MATCH rather than merely being present \u2014 a non-empty string that says something different is still a divergence. Confirm the source is the node's own evaluation result, not the seeded contract echoed back. Run the suite as `npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**'` \u2014 a bare run picks up nested worktrees. Verify on a CLEAN checkout, not just the working tree: this repo has twice been green locally and red in CI because uncommitted state was carrying the result.",
    "estimatedFiles": [
      "src/pge/nodes/sprint-review.ts",
      "src/orchestrator/pipeline.ts",
      "src/orchestrator/workflow/conformance.engines.test.ts",
      ".bober/golden/"
    ],
    "estimatedDuration": "medium",
    "iterationHistory": [],
    "createdAt": "2026-08-14T00:00:00Z",
    "updatedAt": "2026-08-14T13:20:31Z"
  },
  "config": {
    "commands": {
      "build": "npm run build",
      "typecheck": "npm run typecheck",
      "typecheckTests": "npm run typecheck:tests",
      "lint": "npm run lint",
      "test": "npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**'",
      "golden": "GOLDEN_PASS_THRESHOLD=80 node scripts/run-golden-regression.mjs"
    },
    "generator": {
      "model": "claude-opus-5",
      "maxTurnsPerSprint": 50,
      "autoCommit": true,
      "branchPattern": "bober/{feature-name}",
      "provider": "anthropic"
    }
  },
  "principles": "# Project Principles\n\n> These principles guide all planning, generation, and evaluation in the Bober pipeline.\n> Updated: 2026-04-16\n\n## Mission\n\nAgent-bober is a multi-agent harness that builds applications autonomously using any LLM. It orchestrates a pipeline of specialized agents (Researcher, Planner, Curator, Generator, Evaluator) and supports Claude, GPT, Gemini, and Ollama. It ships as an npm CLI + MCP server for Claude Code, Cursor, and Windsurf.\n\n## Users\n\n- **Primary:** Developers using Claude Code, Cursor, or Windsurf who want autonomous feature generation with quality gates\n- **Secondary:** Teams integrating agent-bober programmatically via the TypeScript API or MCP server\n- Technical level: intermediate to advanced \u2014 users understand LLMs, CI/CD, and modern JS tooling\n\n## Quality Standards\n\n- **Type safety:** TypeScript strict mode with all strict flags (`noUnusedLocals`, `noUnusedParameters`, `noImplicitReturns`, `noFallthroughCasesInSwitch`, `isolatedModules`). Zero type errors is a hard gate.\n- **Lint compliance:** ESLint flat config with `consistent-type-imports` enforced, `no-explicit-any` warned, unused vars errored (with `_` prefix escape). Zero lint errors is a hard gate.\n- **Test coverage:** Vitest for unit tests. Tests are collocated with source (`*.test.ts` next to `*.ts`). Tests run against the real project when practical (e.g., scanner tests scan agent-bober itself).\n- **Build integrity:** `tsc` must produce clean output. Build is a required evaluator strategy.\n\n## Technical Principles\n\n### Follow\n\n- **ESM everywhere.** `\"type\": \"module\"` in package.json. All imports use `.js` extensions for NodeNext resolution. No CommonJS.\n- **Provider-agnostic interfaces.** All LLM interaction goes through `providers/types.ts` \u2014 provider-specific SDKs are wrapped by adapters (`anthropic.ts`, `openai.ts`, `google.ts`). Never leak SDK types outside adapter files.\n- **Zod for config validation.** All configuration schemas are Zod schemas in `config/schema.ts`. Runtime config loading uses `z.parse()`. No hand-rolled validation.\n- **Contract-based architecture.** PlanSpecs, SprintContracts, and EvalResults are the communication protocol between agents. They live in `contracts/` and are JSON files on disk in `.bober/`.\n- **Filesystem state.** All mutable state (specs, contracts, handoffs, eval results, history) is stored as JSON files in `.bober/`. No database, no in-memory global state.\n- **Section comments.** Use unicode box-drawing section headers: `// -- Section Name ------` to organize long files.\n- **Small utility modules.** Utils in `utils/` are focused single-purpose files (`fs.ts`, `git.ts`, `logger.ts`). Keep them small.\n- **Conventional commits.** Format: `type: description` where type is `feat`, `fix`, `docs`, `release`, `refactor`, `test`, `chore`. Sprint-generated commits use `bober(sprint-N): description`.\n- **Use `type` imports.** ESLint enforces `consistent-type-imports` -- import types with `import type { ... }` syntax.\n- **Prefix unused params with `_`.** The `_` prefix is the only escape hatch for unused variables/parameters.\n\n### Avoid\n\n- **No `any` without justification.** `no-explicit-any` is a warning; aim for zero. Use `unknown` + type narrowing instead.\n- **No SDK lock-in.** Never import `@anthropic-ai/sdk` or `openai` outside of their respective adapter files in `providers/`.\n- **No synchronous filesystem ops.** All fs operations use `node:fs/promises`. No `fs.readFileSync` etc.\n- **No barrel re-exports for deep internals.** `src/index.ts` exports the public API only. Internal modules import directly.\n- **No test mocks for filesystem.** Tests that need filesystem state create temp directories and clean up. The scanner tests run against the real codebase.\n\n## Design Principles\n\nN/A -- no user interface. Agent-bober is a CLI tool and programmatic library. The MCP server exposes tools, not UI.\n",
  "evaluatorFeedback": {
    "failedCriteria": [
      "sc-5-3"
    ],
    "passedCriteria": [
      "sc-5-1",
      "sc-5-2",
      "sc-5-4",
      "sc-5-5",
      "sc-5-6"
    ],
    "detail": {
      "criterionId": "sc-5-3",
      "required": true,
      "result": "FAIL",
      "evidence": "The strip at sprint-review.ts:276-279 is real and load-bearing \u2014 evaluator confirmed independently. But the shipped negative control does NOT bite.",
      "feedback": "MUTATION TEST: replaced `...contractWithoutFeedback` with `...contract` at sprint-review.ts:279 and deleted the destructure. ALL 32 tests in sprint-evaluate.test.ts still passed, INCLUDING the 'sc-5-3 negative control'. Reason: that test only exercises a PASSING branch where outcome.evaluatorFeedback is always defined, so spread-order alone (later key wins) overrides the seed whether or not it was stripped. The 4th test uses the fixture default contract which has no seeded key at all. Across the WHOLE repo only one test failed on the mutant \u2014 an unrelated golden byte-order diff (capture.test.ts:302) \u2014 and only from JSON key insertion order, not semantics. Evaluator constructed the discriminating test itself: seed a stale value AND route through underDeliveringExplain(1)'s refusal path where outcome.evaluatorFeedback is genuinely undefined \u2014 that test FAILS on the mutant and PASSES on shipped code. Also a definitionOfDone violation: the doc comment at sprint-review.ts:266-274 claims 'a stale value on contract (however it got there) can never survive by omission' with no test that fails when the claim stops being true."
    },
    "generatorFeedback": [
      {
        "priority": "high",
        "category": "quality",
        "file": "src/pge/nodes/sprint-evaluate.test.ts",
        "line": "860-889",
        "description": "The sc-5-3 negative control does not discriminate between the strip being present or absent.",
        "expected": "A test that seeds a stale evaluatorFeedback/generatorNotes AND routes through a path where the decisive verdict carries NO raw value (e.g. the refusal path, or security-block-into-exhaustion), then asserts the settled contract does not carry the stale seed.",
        "reproduction": "At 080967b: change sprint-review.ts:279 from `...contractWithoutFeedback,` to `...contract,`, delete the destructure at 276-278, run `npx vitest run src/pge/nodes/sprint-evaluate.test.ts` \u2014 all 32 tests still pass."
      }
    ],
    "orchestratorNote": "Only sc-5-3 failed, and NOT because the implementation is wrong \u2014 the evaluator independently confirmed the strip at sprint-review.ts:276-279 is real and load-bearing. What failed is the PROOF. Do not rewrite the working implementation. Add the discriminating test. Everything else in this sprint passed and must not regress."
  }
}