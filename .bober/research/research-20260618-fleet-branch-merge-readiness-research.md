# Research: Merge-readiness of branch bober/fleet-expand-decomposer into main

**Research ID:** research-20260618-fleet-branch-merge-readiness
**Generated:** 2026-06-18T11:15:15Z
**Questions Explored:** 7
**Files Explored:** 9 (+ orchestrator-verified git facts)

---

## Architecture Overview

Branch `bober/fleet-expand-decomposer` forked from main at `5947709` (merge-base `594770959937…`, = PR #48 "memory self improve p0"). It is **13 commits ahead** of `origin/main` and **1 commit behind** (`6ad4ce9` — docs-only, README.md + docs/providers.md, "clarify DeepSeek API key setup (#49)"). The branch touches only `src/fleet/` and docs: 6 new TS source files, 5 new TS test files, COMMANDS.md (extended 417→710 lines), docs/sprints/README.md, and 6 sprint doc files. **No `skills/`, `agents/`, or `src/cli/index.ts` changes.** New subcommands (`expand`, `expand-deep` + `--critique`) register inside the already-existing `registerFleetCommand` in `src/cli/index.ts`.

## Existing Patterns

- src/fleet/index.ts:130-257 — runFleetExpand: credential fail-fast → decompose → atomic write (tmp+rename) → print → optional --yes → runFleet
- src/fleet/index.ts:259-393 — runFleetExpandDeep: same pattern, calls decomposeGoalDeep; `--critique` opt-in at :331 (guarded spread)
- src/fleet/index.ts:530-531 — registerFleetCommand wires both expand + expand-deep subcommands
- src/fleet/decomposer.ts — single-stage (Phase 2); DECOMPOSE_MAX_RETRIES=1
- src/fleet/decomposer-deep.ts — two-stage plan+expand (Phase 3); DEEP_MAX_TOTAL_CALLS=4
- src/fleet/critic-deep.ts — fresh-critic gate (Phase 4); CRITIQUE_MAX_ROUNDS=1, DEEP_CRITIQUE_MAX_TOTAL_CALLS=8; CritiqueVerdictSchema = {verdict: approve|reject, feedback}

## Key Files

src/fleet/{index.ts, decomposer.ts, decomposer-deep.ts, critic-deep.ts} + collocated tests (expand.test.ts 19, expand-deep.test.ts 23, expand-deep-critique.test.ts 13, decomposer.test.ts 22, decomposer-deep.test.ts 31, critic-deep.test.ts 42). COMMANDS.md + docs/sprints/* records.

## Integration Points

src/cli/index.ts already calls registerFleetCommand(program) — no change needed. Default output path `<root>/.bober/fleet-expand.json` is SHARED by expand and expand-deep. Credential validation (createClient/validateApiKey, DEEPSEEK_API_KEY) is synchronous before any FS write.

## Test Coverage

`npx vitest run src/fleet/` → 14 files, 188 tests, all pass (0 failures). All fleet tests use DI seams (no real LLM calls). The 6 known-failing tests/e2e/cockpit-integration.test.ts (MCP "Connection closed") are pre-existing and unrelated (not run in the fleet suite).

## Risk Areas

- **Merge conflict surface: ZERO.** `git merge-tree origin/main HEAD` reports no conflicts. Main's only new commit (6ad4ce9) touches README.md + docs/providers.md; the branch touches neither (no file overlap).
- **Plugin cache staleness:** installed_plugins.json pins bober@agent-bober at **v0.17.0 (commit 3852ede, 2026-06-13)**. The cached plugin's `src/fleet/` has NO decomposer*/critic-deep*/expand* files; COMMANDS.md cache=417 vs working tree=710. **Skills + agents dirs are IDENTICAL cache vs working tree** (no fleet-specific skill introduced) — the plugin's skill/agent surface is NOT stale for this branch; only its bundled-source snapshot predates the fleet work.
- **Shared default output path:** expand and expand-deep both default to `<root>/.bober/fleet-expand.json`; concurrent use with default --out would clobber (atomic write guards torn writes, not path collision).
- **Medical code: ABSENT.** src/medical/ does not exist on this branch; 0 tracked medical files, 0 medical commits since fork. Medical-team lives only on the separate unmerged branch bober/medical-team. (.bober/ has untracked medical artifacts from prior sessions — not staged, not in the committed diff.)

---

*Generated by bober.research — factual findings only, no implementation recommendations.*
