{
  "contractId": "sprint-spec-20260812-pge-real-workload-errors-3",
  "specId": "spec-20260812-pge-real-workload-errors",
  "sprintNumber": 3,
  "title": "Size the caps to the corpus, regenerate the artifact, pin both directions",
  "description": "The decided remedy: raise every implicated channel's maxInlineBytes to a value derived from the committed corpus, keep StateBloatError as a guard against genuinely runaway writes, regenerate the artifact and the state audit, take a MINOR graphVersion bump with a changelog entry, and pin each cap two-directionally against the corpus so that neither shrinking nor inflating one can happen silently.",
  "status": "proposed",
  "dependsOn": [
    "sprint-spec-20260812-pge-real-workload-errors-2"
  ],
  "features": [
    "feat-2"
  ],
  "successCriteria": [
    {
      "criterionId": "sc-3-1",
      "description": "src/pge/topology/coding.graph.ts declares, for every channel the measurement implicates, a maxInlineBytes at least the corpus maximum for that channel, and .bober/topology/coding.json is regenerated from it via `pge dump`.",
      "verificationMethod": "unit-test",
      "required": true
    },
    {
      "criterionId": "sc-3-2",
      "description": "graphVersion takes a MINOR bump and docs/pge-graph.md gains a changelog entry keyed by it, stating which caps moved and the measured basis for each.",
      "verificationMethod": "unit-test",
      "required": true
    },
    {
      "criterionId": "sc-3-3",
      "description": "`pge dump --check`, `pge validate --mode full`, `pge docs --check`, `pge diff --require-version-bump` and `pge audit-state` followed by `git diff --exit-code` all pass against the regenerated artifact and the regenerated state audit.",
      "verificationMethod": "unit-test",
      "required": true
    },
    {
      "criterionId": "sc-3-4",
      "description": "A test pins each cap two-directionally against the committed corpus: lowering a cap below its corpus maximum FAILS, and raising a cap with no corpus payload justifying it FAILS. Both directions proven by mutation.",
      "verificationMethod": "unit-test",
      "required": true
    },
    {
      "criterionId": "sc-3-5",
      "description": "A negative control proves StateBloatError still bites: a value above the NEW cap is still rejected and the write is still dropped, so the check was re-sized rather than removed.",
      "verificationMethod": "unit-test",
      "required": true
    },
    {
      "criterionId": "sc-3-6",
      "description": "The golden dataset still passes unchanged -- a MINOR bump does not invalidate cases, and no golden file is edited in this sprint.",
      "verificationMethod": "unit-test",
      "required": true
    },
    {
      "criterionId": "sc-3-7",
      "description": "Full suite, typecheck, typecheck:tests, lint and build are green.",
      "verificationMethod": "build",
      "required": true
    }
  ],
  "nonGoals": [
    "Removing or weakening the StateBloatError check.",
    "Editing any golden case -- if a golden case fails here, that is a finding, not a licence to re-capture.",
    "Adding the specDraft channel, which is sprint 7."
  ],
  "stopConditions": [
    "The golden dataset fails after the cap change -- stop and report; a MINOR cap bump should not move any recorded artifact, and if it does, that is a defect worth understanding before proceeding.",
    "`pge diff` reports the change as MAJOR rather than MINOR -- stop and reconsider the classification rather than forcing the bump."
  ],
  "definitionOfDone": "All required criteria met, the full suite plus typecheck, typecheck:tests, lint and build are green, and every documentation claim added is backed by a test that fails when the claim stops being true.",
  "assumptions": [],
  "outOfScope": [],
  "ambiguityScore": 3,
  "generatorNotes": "Edit the AUTHORED literal at src/pge/topology/coding.graph.ts:113-190, then regenerate the committed JSON with the pge CLI -- never hand-edit .bober/topology/coding.json, which `pge dump --check` will catch anyway. Pick caps from the corpus with deliberate headroom and say in the code comment what the headroom is for; a cap that exactly equals today's maximum will fail the day a spec grows one field. Keep StateBloatError: the point is to size the guard, not to delete it. Remember the state audit is a committed artifact gated by `pge audit-state` plus `git diff --exit-code` (src/pge/audit-git-gate.test.ts), so regenerate it in the same commit.",
  "evaluatorNotes": "Verify the two-directional pin genuinely bites in BOTH directions by mutating it yourself -- lower a cap below corpus max and confirm failure, then raise one arbitrarily and confirm failure. A pin that only catches shrinkage is half a pin. Verify the negative control for StateBloatError by constructing a value above the new cap and confirming the write is dropped and a StateBloatError lands in `rejected`. Confirm .bober/topology/coding.json was regenerated rather than hand-edited by running `pge dump --check` yourself. Confirm the changelog entry states a MEASURED basis per cap, not a round number with no provenance.",
  "estimatedFiles": [
    "src/pge/topology/coding.graph.ts",
    ".bober/topology/coding.json",
    ".bober/topology/state-audit.json",
    "docs/pge-graph.md",
    "src/pge/golden/workload.test.ts"
  ],
  "estimatedDuration": "medium",
  "iterationHistory": [],
  "createdAt": "2026-08-12T00:00:00Z",
  "updatedAt": "2026-08-12T00:00:00Z"
}