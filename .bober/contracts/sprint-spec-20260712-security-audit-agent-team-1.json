{
  "contractId": "sprint-spec-20260712-security-audit-agent-team-1",
  "specId": "spec-20260712-security-audit-agent-team",
  "sprintNumber": 1,
  "title": "Add security config section, audit result types, and .bober/security store",
  "description": "Lay the typed foundation for the security auditor: an opt-in default-off SecuritySectionSchema wired as an OPTIONAL key on BoberConfigSchema (existing configs byte-identical), the SecurityAuditResult/SecurityFinding/VulnClass types wrapping the LOCKED ReviewResult/ReviewFinding shapes, a pure verdict-derivation function, and a security-audit-state store persisting human-readable markdown to .bober/security/ (separate from .bober/reviews/ per ADR-3).",
  "status": "completed",
  "createdAt": "2026-07-12T09:30:00.000Z",
  "updatedAt": "2026-07-12T10:38:29.000Z",
  "completedAt": "2026-07-12T10:38:29.000Z",
  "dependsOn": [],
  "features": [
    "feat-1"
  ],
  "successCriteria": [
    {
      "criterionId": "sc-1-1",
      "description": "SecuritySectionSchema exists in src/config/schema.ts with fields enabled (default false), failClosed (default true), timeoutMs (default 300000), model (default 'opus'), maxTurns (default 20), provider?/endpoint?/providerConfig?, budget? (reusing BudgetSectionSchema), scanners (z.array(EvalStrategySchema) default []), standaloneBlockOn (enum 'critical'|'important' default 'critical'), hub (boolean default true); it is wired as security: SecuritySectionSchema.optional() on BoberConfigSchema.",
      "verificationMethod": "unit-test",
      "required": true
    },
    {
      "criterionId": "sc-1-2",
      "description": "Every existing config fixture and the repo's own bober.config.json parse to output deep-equal to the pre-change parse (no security key injected, no defaults materialized when the section is absent); all pre-existing config tests pass unchanged.",
      "verificationMethod": "unit-test",
      "required": true
    },
    {
      "criterionId": "sc-1-3",
      "description": "src/orchestrator/security-audit-types.ts exports SecurityFinding (extends ReviewFinding with optional vulnClass), the VulnClass union ('injection'|'authn-authz'|'secret-handling'|'input-validation'|'path-traversal'|'privilege-escalation'), SecurityAuditResult {review, stack, scannerRan, parsed, verdict}, and a pure deriveVerdict(review) returning 'blocked' iff review.critical.length > 0 \u2014 importing ReviewResult/ReviewFinding type-only from code-reviewer-agent.ts, never redefining them.",
      "verificationMethod": "unit-test",
      "required": true
    },
    {
      "criterionId": "sc-1-4",
      "description": "src/state/security-audit-state.ts exports saveSecurityAudit/readSecurityAudit/listSecurityAudits over .bober/security/<contractId>-security-audit.md: save renders via the existing review markdown renderer applied to result.review, read of a missing id returns null without throwing, list returns saved contract ids; round-trip verified in a temp directory.",
      "verificationMethod": "unit-test",
      "required": true
    },
    {
      "criterionId": "sc-1-5",
      "description": "npm run build and npm run typecheck pass; ESLint reports zero errors on new and modified files.",
      "verificationMethod": "build",
      "required": true
    }
  ],
  "nonGoals": [
    "Do not implement runSecurityAudit, the gate, the CLI, scanners, or hub emission (sprints 2-6).",
    "Do not modify ReviewResult/ReviewFinding, review-state.ts, or any code-reviewer behavior (locked).",
    "Do not add security config to createDefaultConfig or any preset \u2014 the section is opt-in only.",
    "Do not touch bober.config.json in this sprint (dogfooding is sprint 7)."
  ],
  "stopConditions": [
    "All five success criteria verified; full existing suite green with zero regressions.",
    "Config byte-identity proven by deep-equal parse tests, not by assertion of absence alone."
  ],
  "definitionOfDone": "The security config section, wrapper types, verdict derivation, and .bober/security/ store exist, fully tested, with every existing config parsing byte-identically and the full suite green.",
  "assumptions": [
    "BudgetSectionSchema, EvalStrategySchema, and ModelChoiceSchema are exported (or exportable) from src/config/schema.ts as cited at schema.ts:48-51 and :74-88 in the architecture; if ModelChoiceSchema has a different name, reuse whatever enum the reviewer/evaluator sections use for their model field.",
    "The review markdown renderer (renderReviewMarkdown or equivalent) is exported from code-reviewer-agent.ts or its render module; if it is module-private, export it additively rather than duplicating it.",
    "review-state.ts (src/state/review-state.ts:12-15) is the store pattern to mirror: async fs/promises only, ensureDir before write."
  ],
  "outOfScope": [
    "Audit execution, gate wiring, CLI, scanner pre-filter, hub emission, skill, dogfooding (later sprints)."
  ],
  "ambiguityScore": 2,
  "generatorNotes": "Anchors may have drifted \u2014 re-locate by pattern. (1) src/config/schema.ts: find the existing default-off section precedents (evaluator panel {enabled:false}, reviewer section, medical egress axes) and follow their exact style; place SecuritySectionSchema near the other role sections and wire `security: SecuritySectionSchema.optional()` onto BoberConfigSchema \u2014 CRITICAL: .optional() on the section, no top-level default, so absent stays absent. Include standaloneBlockOn (z.enum(['critical','important']).default('critical')) and hub (z.boolean().default(true)) \u2014 these are consumed by sprints 4 and 6 but belong to the schema now to avoid three schema-touch sprints. (2) src/orchestrator/security-audit-types.ts: import type { ReviewResult, ReviewFinding } from './code-reviewer-agent.js' (verify the actual export names at code-reviewer-agent.ts:17-37). deriveVerdict must be pure and exported for reuse by the core, gate, and CLI. (3) src/state/security-audit-state.ts: mirror review-state.ts structurally (same helper usage, ensureDir, node:fs/promises); filename pattern `${contractId}-security-audit.md`. (4) Tests collocated (*.test.ts): config deep-equal paired-parse test, verdict derivation table test, store round-trip in a temp dir (repo convention: real fs in temp dirs, no fs mocks). Use unicode box section comments per principles.md.",
  "evaluatorNotes": "For sc-1-2: run the FULL config test file plus parse the repo's real bober.config.json before/after (checkout the schema module's behavior with the section absent) and deep-equal the outputs \u2014 do not accept a test that only checks 'security' in result. For sc-1-3: verify vulnClass is optional and that ReviewFinding/ReviewResult are imported type-only (grep for 'import type'); table-test deriveVerdict with critical empty/non-empty including important-only findings \u2192 'pass'. For sc-1-4: exercise read-of-missing \u2192 null (not throw) explicitly. Confirm no sync fs calls (grep readFileSync/writeFileSync in new files). Run build + typecheck + lint + full suite.",
  "estimatedFiles": [
    "src/config/schema.ts",
    "src/config/schema.test.ts",
    "src/orchestrator/security-audit-types.ts",
    "src/orchestrator/security-audit-types.test.ts",
    "src/state/security-audit-state.ts",
    "src/state/security-audit-state.test.ts"
  ],
  "estimatedDuration": "medium",
  "iterationHistory": [
    {
      "iteration": 1,
      "result": "pass",
      "evalId": "eval-sprint-spec-20260712-security-audit-agent-team-1-1",
      "timestamp": "2026-07-12T10:38:29.000Z"
    }
  ],
  "lastEvalId": "eval-sprint-spec-20260712-security-audit-agent-team-1-1"
}