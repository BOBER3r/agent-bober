{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "echo 'Files modified — run /bober:eval to verify'"
      },
      {
        "matcher": "Edit|Write",
        "command": "node scripts/graph-hook.mjs"
      }
    ]
  }
}
